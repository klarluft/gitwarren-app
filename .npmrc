# Turn the `engines` ranges in package.json into an error rather than a
# warning. Without this, an npm too old to write a correct lockfile installs
# happily and the damage only surfaces in CI on someone else's machine.
engine-strict=true
