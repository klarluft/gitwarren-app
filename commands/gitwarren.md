---
description: Open the GitWarren review for the current branch and hand over the link
argument-hint: [base ref]
---

Open, or reuse, the GitWarren review for the current branch of the repository in
the working directory, exactly as the gitwarren skill describes: find or add the
repository, one review per branch, uncommitted work included.

Base ref: $ARGUMENTS if given, otherwise the trunk.

Reply with one sentence on what the review contains, then the review link on
its own line.
